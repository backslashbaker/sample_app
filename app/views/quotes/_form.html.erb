<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quote %>
    <%= form.text_field :quote, id: :quote_quote %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :quote_author %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
